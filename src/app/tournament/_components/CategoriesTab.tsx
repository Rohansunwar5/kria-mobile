import { View, Text } from 'react-native';
export function CategoriesTab(_: { tournamentId: string; tournamentStatus: string }) {
  return <View className="px-5 py-6"><Text className="font-montserrat text-gray-400">Categories…</Text></View>;
}
