import { View, Text } from 'react-native';

export default function Home() {
  return (
    <View className="flex-1 items-center justify-center bg-ink">
      <Text className="font-oswald text-2xl text-brand">Kria — Home</Text>
      <Text className="mt-2 font-montserrat text-white">Tournament browse arrives in Phase 2</Text>
    </View>
  );
}
