# Expo SDK 54

This app targets **Expo SDK 54** (pinned to match the Expo Go version available in the app stores, so the app can be tested on a physical device via Expo Go).

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code. Do not upgrade the SDK without confirming the target Expo Go version still matches.
